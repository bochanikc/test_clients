class Common:
    class Alerts:
        class FailMessage:
            fail_message = {'css': 'div.invalid-tooltip'}
            fail_message_from_server = {'css': '.invalid-feedback-from-server'}
            fail_message_on_front = {'css': 'div.invalid-feedback'}
